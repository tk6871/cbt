(function(){"use strict";let t=[];self.addEventListener("message",n=>{const e=n.data;if(e.type==="index"){t=e.entries||[],self.postMessage({type:"ready",count:t.length});return}const i=String(e.query||"").trim().toLocaleLowerCase("ko");if(i.length<2){self.postMessage({type:"results",ids:[]});return}const a=i.split(/\s+/).filter(Boolean),l=t.filter(s=>!e.qualificationKey||s.qualificationKey===e.qualificationKey).filter(s=>a.every(r=>s.haystack.includes(r))).slice(0,120).map(s=>s.id);self.postMessage({type:"results",ids:l})})})();
