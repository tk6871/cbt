# 작업 장치 선택 원칙

- 사용자는 Windows PC와 MacBook Pro M4 Pro(메모리 24GB)를 함께 사용한다.
- 작업을 시작하기 전에 MacBook에서 실행하는 편이 시간, 성능, 안정성 면에서 의미 있게 유리한지 먼저 판단한다.
- MacBook이 더 효율적인 작업이라면 Windows에서 바로 실행하지 말고, 먼저 사용자에게 MacBook에서 진행할지 확인한다. 사용자가 맥에서 새 명령을 내릴 수 있도록 필요한 저장소 상태와 시작 문구도 안내한다.
- 이미지 초해상도·대량 이미지 변환·OCR·PDF 일괄 처리·AI/머신러닝 모델 실행·장시간 빌드처럼 연산량이 큰 작업은 특히 장치 적합성을 먼저 비교한다.
- Windows에서 작업하던 중 MacBook이 더 적합하다는 사실을 발견하면, 중복 작업을 계속하지 않는다. 현재 결과를 안전하게 저장할 수 있는 지점에서 멈추고 사용자에게 처음부터 MacBook에서 처리하는 편이 나은 이유를 설명한다.
- 짧은 코드 수정, 데이터 한두 건 교정, 상태 확인처럼 장치 차이가 거의 없는 작업은 현재 장치에서 바로 진행할 수 있다.

# 장치 간 작업 인수인계

- 작업을 시작할 때 저장소 루트의 `WORK_STATE.md`를 먼저 읽고 현재 완료 상태, 남은 작업, 검증 결과, 장치별 도구 차이를 확인한다.
- 코드·문제 데이터·정답·해설·이미지 자산·빌드 설정·배포 설정처럼 프로젝트 상태를 실질적으로 바꾼 작업은 종료 전에 `WORK_STATE.md`를 최신 상태로 갱신한다.
- `WORK_STATE.md`에는 실제로 완료한 내용, 실제로 실행한 검증, 남은 작업, 다음 장치에서 필요한 도구와 주의사항만 기록한다. 실행하지 않은 테스트나 확인하지 않은 결과를 완료로 적지 않는다.
- 사용자 화면이나 배포본에 영향을 주는 변경은 필요에 따라 `CHANGELOG.md`도 함께 갱신한다. 관리자 방문 기록이나 비공개 분석 기능의 세부 내용은 사용자가 명시적으로 요청하지 않는 한 공개 패치노트에 넣지 않는다.
- 읽기 전용 조사, 상태 확인, 설명만 한 작업이나 사소한 임시 변경은 `WORK_STATE.md` 갱신을 생략할 수 있다.
- Windows에서 작업을 마치면 관련 코드와 `WORK_STATE.md`를 함께 커밋·푸시하고, Mac에서는 작업 전에 Pull한다. Mac에서 작업한 경우도 반대로 동일하게 적용한다.
- 두 장치에서 같은 파일을 동시에 수정하지 않는다. 장치를 바꾸기 전에 현재 장치의 미커밋 변경과 원격 동기화 상태를 확인한다.
- `WORK_STATE.md`, `AGENTS.md`, `README.md`, `CHANGELOG.md`에는 비밀번호, API 비밀키, 인증 토큰, SSH 개인키, 개인 IP 주소 같은 민감정보를 기록하지 않는다.

# 사용자 작업 원칙

- 사용자는 한국어로 쉽고 명확한 설명을 선호한다. 특히 문제 해설은 초보자가 이해할 수 있도록 핵심 원리와 계산 순서를 풀어서 설명한다.
- 기존 사용자 변경사항과 원본 문제 이미지는 명시적인 지시 없이 삭제하거나 되돌리지 않는다.
- 공유용 압축 파일은 만들지 않는다. 압축은 사용자가 직접 수행한다.
- 대량 다운로드·새 도구 설치·대량 이미지 변환·처리 방식 변경은 실행 전에 예상 범위와 결과 차이를 간단히 보고한다.
